/**
 * @fileOverview Identity properties and data.
 */

define(['model/identity'], function(Identity) {
  describe('Identity model', function() {

    it('is created properly');

    it('changes password');
    
    it('generates'); // private
  });
});

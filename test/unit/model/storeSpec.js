/**
 * @fileOverview Mapping coordinating with the app's database.
 */

define(['model/store'], function(Store) {
  describe('Storage model', function() {
    it('creates properly');

    it('gets a value from a key');

    it('sets a key value pair into the store');

    it('sets value to default if not defined');

    it('saves the store to database');

    it('gets private data');

    it('sets private data');
  });
});